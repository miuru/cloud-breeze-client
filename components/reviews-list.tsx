'use client'; import { useState } from 'react'; import { reviews } from '@/data/reviews'; import { Card } from '@/components/ui';
export default function ReviewsList(){const [count,setCount]=useState(6);return <><div className="grid gap-5 md:grid-cols-2">{reviews.slice(0,count).map(r=><Card key={r.guestName} className="p-6"><div className="flex justify-between gap-3"><div><h3 className="text-xl">{r.guestName}</h3><p className="mt-1 text-sm text-slate-500">{r.country} · {r.stayType}</p></div><span className="h-fit rounded-lg bg-[#003b95] px-3 py-2 font-bold text-white">{r.rating}</span></div><p className="mt-5 leading-7 text-slate-700">“{r.reviewText}”</p><p className="mt-5 text-xs text-slate-500">Stayed {r.date}</p></Card>)}</div>{count<reviews.length&&<button onClick={()=>setCount(reviews.length)} className="mt-8 rounded-full border border-teal px-5 py-3 font-bold text-teal">Load more reviews</button>}</>}
