'use client'; import { useState } from 'react'; import Image from 'next/image'; import { attractions } from '@/data/attractions'; import { Card } from '@/components/ui';
export default function AttractionsList(){const [filter,setFilter]=useState('All');const types=['All', ...Array.from(new Set(attractions.map(x => x.category)))]; const shown=filter==='All'?attractions:attractions.filter(x=>x.category===filter);return <><div className="mb-8 flex flex-wrap gap-2">{types.map(t=><button key={t} onClick={()=>setFilter(t)} className={`rounded-full px-4 py-2 text-sm font-bold ${filter===t?'bg-teal text-white':'bg-white text-teal ring-1 ring-teal/20'}`}>{t}</button>)}</div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{shown.map(a=><Card key={a.name}><Image width={700} height={480} className="h-48 w-full object-cover" src={a.image} alt={a.name}/><div className="p-5"><div className="flex justify-between gap-3"><p className="eyebrow">{a.category}</p><p className="text-xs font-bold text-ocean">{a.distance}</p></div><h3 className="mt-2 text-xl">{a.name}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{a.description}</p></div></Card>)}</div></>}
