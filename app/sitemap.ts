import type { MetadataRoute } from 'next'; export default function sitemap(): MetadataRoute.Sitemap { const base='https://cloudbreezevilla.example'; return ['','/villa','/attractions','/gallery','/reviews','/contact','/book'].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:'monthly',priority:path===''?1:0.7})); }
